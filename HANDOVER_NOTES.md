# 開発引き継ぎ資料 (Handover Notes) - 医学部合格アプリ

## 1. プロジェクト概要
受験生（子供）の遊びスマホを制限し、学習時間を確保するためのAndroidアプリ。
- **目標**: 指定した制限時間（デフォルト4時間、週単位減少機能は廃止済み）を超えた場合、および曜日別の「勉強時間」の間、遊びアプリをブロックする。
- **ターゲット端末**: Samsung SCG08 (Android 13/14)

## 2. 技術スタック
- **言語**: Kotlin
- **UI**: Jetpack Compose
- **データ管理**: Jetpack DataStore (Preferences), GSON
- **監視機能**: AccessibilityService (ユーザー補助)
- **統計取得**: UsageStatsManager

## 3. 現在のシステム構成
- `AppMonitorService.kt`: 核心となる監視サービス。1.5秒周期で最前面アプリをチェックし、制限・ブロック・リアルタイム時間計測を行う。
- `LocalSettingsRepository.kt`: 設定と利用統計の永続化を担当。
- `UsageTracker.kt`: `UsageStatsManager` を使用して、Androidシステム側の正確な利用統計を取得する。
- `TimeCalculator.kt`: 残り時間や共通テストまでのカウントダウンを計算するユーティリティ。

## 4. 未解決の課題 (要修正)
### リアルタイム時間計測の不安定さ
- **現象**: 制限時間を1分に設定しても、YouTubeを視聴し続けている間に時間が減らない、あるいはロックがかからない。
- **原因の推測**:
    1. `UsageStatsManager` はアプリが閉じられるまで統計を更新しない性質がある。
    2. `AppMonitorService` に実装した独自計測（1秒ずつの加算）と、リポジトリの同期が正しく行われていない可能性がある。
    3. 手動設定時の「基準点（Baseline）」の計算が複雑化しており、表示上の0分と内部のブロック判定が一致していない。

## 5. 次の開発者への依頼事項
1. **計測の完全リアルタイム化**:
   `AppMonitorService` 内の `accumulatedSessionMs` を使い、システム統計を待たずに「今まさに開いている秒数」でロックをかけるロジックを完成させてください。
2. **計算式の単純化**:
   `calculateRemainingMs` 関数を、現在の累積値と設定値の比較だけに整理し、デバッグしやすい形にしてください。
3. **通知の安定表示**:
   Android 13以降での通知許可フローを確実にし、監視サービスがOSに殺されないようにしてください。

## 6. 特記事項
- **アンインストール防止**: 設定画面の一部（アンインストールボタン等）を監視してブロックする機能が実装されています。
- **PiP回避**: YouTubeの小窓（PiP）を強制停止するために `AudioManager` で再生停止命令を送る処理を `BlockActivity` に入れています。
- **Windows版**: 別途 `WINDOWS_VERSION_PLAN.md` に基づいたPC版の開発が予定されています。
