<Window x:Class="MedicalSchoolApp.Windows.Views.PasswordPromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="保護者パスワード" Height="220" Width="360"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        ShowInTaskbar="True" Topmost="True"
        Background="#0F172A">
    <Grid Margin="20">
        <StackPanel VerticalAlignment="Center">
            <TextBlock x:Name="MessageText" Text="保護者パスワードを入力してください" Foreground="White" Margin="0,0,0,12" TextWrapping="Wrap"/>
            <PasswordBox x:Name="PasswordBox" Padding="6" Background="#1E293B" Foreground="White" BorderBrush="#334155"
                         KeyDown="PasswordBox_KeyDown" Margin="0,0,0,8"/>
            <TextBlock x:Name="ErrorText" Foreground="#EF4444" Visibility="Collapsed" Margin="0,0,0,8"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="8"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Content="OK" Height="36" Background="#3B82F6" Foreground="White" BorderThickness="0" Click="OkButton_Click"/>
                <Button Grid.Column="2" Content="キャンセル" Height="36" Background="#334155" Foreground="White" BorderThickness="0" Click="CancelButton_Click"/>
            </Grid>
        </StackPanel>
    </Grid>
</Window>
