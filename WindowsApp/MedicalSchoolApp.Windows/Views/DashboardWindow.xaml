<Window x:Class="MedicalSchoolApp.Windows.Views.DashboardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="医学部合格アプリ" Height="520" Width="760"
        WindowStartupLocation="CenterScreen"
        Background="#0F172A">
    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="ダッシュボード" FontSize="24" FontWeight="Bold" Foreground="White"/>
            <Border Grid.Column="1" x:Name="ModeBadgeBorder" CornerRadius="14" Padding="14,6" Background="#10B981">
                <TextBlock x:Name="ModeBadgeText" Text="通常" Foreground="White" FontWeight="Bold"/>
            </Border>
        </Grid>

        <!-- Remaining time -->
        <Border Grid.Row="1" Background="#1E293B" CornerRadius="16" Padding="24" Margin="0,0,0,16">
            <StackPanel>
                <TextBlock Text="今日の残り時間" Foreground="#94A3B8" FontSize="14" Margin="0,0,0,8"/>
                <TextBlock x:Name="RemainingMinutesText" Text="60 分" Foreground="White" FontSize="40" FontWeight="Bold"/>
            </StackPanel>
        </Border>

        <!-- Next study / exam countdown -->
        <Grid Grid.Row="2" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Border Grid.Column="0" Background="#1E293B" CornerRadius="16" Padding="20">
                <StackPanel>
                    <TextBlock Text="次の勉強時間" Foreground="#94A3B8" FontSize="14" Margin="0,0,0,8"/>
                    <TextBlock x:Name="NextStudySlotText" Text="予定なし" Foreground="White" FontSize="18" FontWeight="SemiBold" TextWrapping="Wrap"/>
                </StackPanel>
            </Border>
            <Border Grid.Column="2" Background="#1E293B" CornerRadius="16" Padding="20">
                <StackPanel>
                    <TextBlock Text="共通テストまで" Foreground="#94A3B8" FontSize="14" Margin="0,0,0,8"/>
                    <TextBlock x:Name="ExamCountdownText" Text="-" Foreground="White" FontSize="18" FontWeight="SemiBold" TextWrapping="Wrap"/>
                </StackPanel>
            </Border>
        </Grid>

        <Grid Grid.Row="3"/>

        <!-- Buttons -->
        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" x:Name="OpenChatGptButton" Content="💬 ChatGPTを開く" Height="48" FontSize="15" Click="OpenChatGptButton_Click"
                    Background="#3B82F6" Foreground="White" BorderThickness="0"/>
            <Button Grid.Column="2" x:Name="OpenSettingsButton" Content="⚙️ 保護者設定" Height="48" FontSize="15" Click="OpenSettingsButton_Click"
                    Background="#334155" Foreground="White" BorderThickness="0"/>
        </Grid>
    </Grid>
</Window>
